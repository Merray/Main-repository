import { ClientCategoryPipe } from './client-category.pipe';

describe('ClientCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new ClientCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
