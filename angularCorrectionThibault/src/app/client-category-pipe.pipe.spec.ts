import { ClientCategoryPipePipe } from './client-category-pipe.pipe';

describe('ClientCategoryPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ClientCategoryPipePipe();
    expect(pipe).toBeTruthy();
  });
});
